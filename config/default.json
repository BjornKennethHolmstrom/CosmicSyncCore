{
  "database": {
    "user": "${DB_USER}",
    "host": "${DB_HOST}",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "port": "${DB_PORT}"
  },
  "p2p": {
    "topic": "cosmicsynccore",
    "bootstrapNodes": [
      // Add bootstrap nodes here
    ]
  }
}
